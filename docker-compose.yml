services:
  qq-farm-bot:
    build:
      context: .
      dockerfile: core/Dockerfile
    container_name: qq-farm-bot
    restart: unless-stopped
    environment:
      ADMIN_PASSWORD: admin
      TZ: Asia/Shanghai
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/core/data
